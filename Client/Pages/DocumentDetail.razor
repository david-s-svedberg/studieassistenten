@page "/document/{DocumentId:int}"
@attribute [Authorize]
@using StudieAssistenten.Client.Services
@using StudieAssistenten.Shared.DTOs
@using StudieAssistenten.Shared.Enums
@inject IDocumentService DocumentService
@inject NavigationManager Navigation
@inject IJSRuntime JSRuntime

<PageTitle>@(document?.FileName ?? "Document")</PageTitle>

@if (isLoading)
{
    <div class="text-center py-5">
        <div class="spinner-border text-light" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
        <p class="mt-3 text-light">Loading document...</p>
    </div>
}
else if (document == null)
{
    <div class="alert alert-danger">
        <h4>Document not found</h4>
        <p>The requested document could not be found.</p>
        <button class="btn btn-primary" @onclick='() => Navigation.NavigateTo("/documents")'>
            Back to Documents
        </button>
    </div>
}
else
{
    <div class="document-detail-container">
        <div class="document-header">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h1 class="document-title">@document.FileName</h1>
                <button class="btn btn-secondary" @onclick="GoBack">
                    <i class="bi bi-arrow-left"></i> Back
                </button>
            </div>
            <div class="document-meta">
                <span class="badge @GetStatusBadgeClass(document.Status)">@document.Status</span>
                <span class="text-muted ms-3">@FormatFileSize(document.FileSizeBytes)</span>
                <span class="text-muted ms-3">Uploaded @document.UploadedAt.ToLocalTime().ToString("MMM d, yyyy HH:mm")</span>
            </div>
        </div>

        <div class="document-content-grid">
            @* File Preview Section *@
            @if (IsImageFile(document.FileName))
            {
                <div class="preview-section">
                    <div class="preview-card">
                        <div class="preview-header">
                            <h5>Image Preview</h5>
                        </div>
                        <div class="preview-body" style="flex-direction: column;">
                            <img src="/api/documents/@DocumentId/view"
                                 alt="@document.FileName"
                                 class="document-image-preview"
                                 @onclick="() => showImageModal = true"
                                 style="max-height: 500px; object-fit: contain; cursor: pointer;" />
                            <p class="text-muted mt-2 small" style="margin-bottom: 0;">Click image to view full size</p>
                        </div>
                    </div>
                </div>
            }
            else if (IsPdfFile(document.FileName))
            {
                <div class="preview-section">
                    <div class="preview-card">
                        <div class="preview-header">
                            <h5>PDF Preview</h5>
                            <a href="/api/documents/@DocumentId/view" target="_blank" class="btn btn-sm btn-outline-light">
                                <i class="bi bi-box-arrow-up-right"></i> Open in New Tab
                            </a>
                        </div>
                        <div class="preview-body">
                            <iframe src="/api/documents/@DocumentId/view" style="width: 100%; height: 600px; border: none;"></iframe>
                        </div>
                    </div>
                </div>
            }

            @* Document Content Section *@
            <div class="text-section">
                <div class="text-card">
                    <div class="text-header">
                        <h5>Document Content</h5>
                        @* Only show Extract Text button for non-text files *@
                        @if (document.Status == DocumentStatus.Uploaded && !IsTextFile(document.FileName))
                        {
                            <button class="btn btn-sm btn-primary" @onclick="ProcessDocument" disabled="@isProcessing">
                                @if (isProcessing)
                                {
                                    <span class="spinner-border spinner-border-sm me-1"></span>
                                }
                                <i class="bi bi-cpu"></i> Extract Text
                            </button>
                        }
                        else if (!string.IsNullOrWhiteSpace(document.ExtractedText) && !isEditing)
                        {
                            <button class="btn btn-sm btn-outline-light" @onclick="StartEditing">
                                <i class="bi bi-pencil"></i> Edit
                            </button>
                        }
                    </div>
                    <div class="text-body">
                        @if (string.IsNullOrWhiteSpace(document.ExtractedText))
                        {
                            <div class="text-center py-5 text-muted">
                                <i class="bi bi-file-text" style="font-size: 3rem;"></i>
                                <p class="mt-3">No text extracted yet.</p>
                                @if (document.Status == DocumentStatus.Uploaded)
                                {
                                    <p>Click "Extract Text" to process this document.</p>
                                }
                                else if (document.Status == DocumentStatus.OcrInProgress)
                                {
                                    <div class="spinner-border text-primary mt-3" role="status"></div>
                                    <p class="mt-2">Processing...</p>
                                }
                            </div>
                        }
                        else
                        {
                            @if (isEditing)
                            {
                                <textarea class="form-control text-editor" rows="25" @bind="editedText"></textarea>
                                <div class="d-flex gap-2 mt-3">
                                    <button class="btn btn-primary" @onclick="SaveText" disabled="@isSaving">
                                        @if (isSaving)
                                        {
                                            <span class="spinner-border spinner-border-sm me-1"></span>
                                        }
                                        Save Changes
                                    </button>
                                    <button class="btn btn-secondary" @onclick="CancelEditing">Cancel</button>
                                </div>
                            }
                            else
                            {
                                <div class="extracted-text-display">
                                    @document.ExtractedText
                                </div>
                            }
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    @* Image Modal for Full Screen View *@
    @if (showImageModal && IsImageFile(document?.FileName ?? ""))
    {
        <div class="image-modal" @onclick="() => showImageModal = false">
            <div class="image-modal-content" @onclick:stopPropagation="true">
                <button class="image-modal-close" @onclick="() => showImageModal = false">
                    <i class="bi bi-x-lg"></i>
                </button>
                <img src="/api/documents/@DocumentId/view" alt="@document?.FileName" class="modal-image" />
            </div>
        </div>
    }
}

<style>
    .document-detail-container {
        color: #ececf1;
        max-width: 1400px;
        margin: 0 auto;
    }

    .document-header {
        margin-bottom: 2rem;
    }

    .document-title {
        color: #ececf1;
        font-size: 1.75rem;
        font-weight: 600;
        margin: 0;
    }

    .document-meta {
        margin-top: 0.5rem;
    }

    .document-content-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
        gap: 2rem;
    }

    .preview-section,
    .text-section {
        min-height: 400px;
    }

    .preview-card,
    .text-card {
        background: #444654;
        border: 1px solid #565869;
        border-radius: 0.5rem;
        overflow: hidden;
    }

    .preview-header,
    .text-header {
        padding: 1rem;
        background: #343541;
        border-bottom: 1px solid #565869;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .preview-header h5,
    .text-header h5 {
        margin: 0;
        color: #ececf1;
        font-size: 1rem;
    }

    .preview-body,
    .text-body {
        padding: 1rem;
    }

    .preview-body {
        display: flex;
        justify-content: center;
        align-items: center;
        background: #2a2b32;
    }

    .text-editor {
        background: #2a2b32;
        color: #ececf1;
        border: 1px solid #565869;
        font-family: 'Consolas', 'Monaco', monospace;
        font-size: 0.9rem;
        resize: vertical;
    }

    .extracted-text-display {
        background: #2a2b32;
        padding: 1rem;
        border-radius: 0.25rem;
        white-space: pre-wrap;
        font-family: 'Consolas', 'Monaco', monospace;
        font-size: 0.9rem;
        line-height: 1.6;
        color: #ececf1;
        max-height: 600px;
        overflow-y: auto;
    }

    .generation-buttons {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
    }

    .content-list {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .content-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem;
        background: #2a2b32;
        border-radius: 8px;
    }

    .content-info h6 {
        margin: 0 0 0.25rem 0;
    }

    .content-actions {
        display: flex;
        gap: 0.5rem;
    }

    .badge {
        padding: 0.35rem 0.65rem;
        font-size: 0.875rem;
    }

    @@media (max-width: 1024px) {
        .document-content-grid {
            grid-template-columns: 1fr;
        }
    }

    /* Image Preview Styles */
    .document-image-preview {
        max-width: 100%;
        max-height: 600px;
        border-radius: 8px;
        cursor: pointer;
        transition: transform 0.2s;
    }

    .document-image-preview:hover {
        transform: scale(1.02);
    }

    /* Image Modal Styles */
    .image-modal {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.9);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 2000;
        padding: 20px;
    }

    .image-modal-content {
        position: relative;
        max-width: 95vw;
        max-height: 95vh;
    }

    .modal-image {
        max-width: 100%;
        max-height: 95vh;
        object-fit: contain;
    }

    .image-modal-close {
        position: absolute;
        top: -40px;
        right: 0;
        background: rgba(255, 255, 255, 0.2);
        border: none;
        color: white;
        font-size: 24px;
        width: 40px;
        height: 40px;
        border-radius: 50%;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: background 0.2s;
    }

    .image-modal-close:hover {
        background: rgba(255, 255, 255, 0.3);
    }

    @@media (max-width: 768px) {
        .image-modal-close {
            top: 10px;
            right: 10px;
        }
    }
</style>

@code {
    [Parameter]
    public int DocumentId { get; set; }

    private DocumentDto? document;
    private bool isLoading = true;
    private bool isProcessing = false;
    private bool isEditing = false;
    private bool isSaving = false;
    private string editedText = string.Empty;
    private bool showImageModal = false;

    protected override async Task OnInitializedAsync()
    {
        await LoadDocument();
    }

    private async Task LoadDocument()
    {
        isLoading = true;
        document = await DocumentService.GetDocumentAsync(DocumentId);
        isLoading = false;
    }

    private async Task ProcessDocument()
    {
        if (document == null) return;

        isProcessing = true;
        StateHasChanged();
        
        var success = await DocumentService.TriggerOcrProcessingAsync(DocumentId);
        
        if (success)
        {
            // Poll continuously until processing completes
            var maxAttempts = 60; // 2 minutes max (60 * 2 seconds)
            var attempts = 0;
            
            while (attempts < maxAttempts)
            {
                await Task.Delay(2000);
                await LoadDocument();
                StateHasChanged();
                
                // Check if processing is complete
                if (document.Status != DocumentStatus.OcrInProgress && 
                    document.Status != DocumentStatus.Uploaded)
                {
                    break;
                }
                
                attempts++;
            }
        }
        
        isProcessing = false;
        StateHasChanged();
    }

    private void StartEditing()
    {
        isEditing = true;
        editedText = document?.ExtractedText ?? string.Empty;
    }

    private void CancelEditing()
    {
        isEditing = false;
        editedText = string.Empty;
    }

    private async Task SaveText()
    {
        if (document == null) return;

        isSaving = true;
        var success = await DocumentService.UpdateExtractedTextAsync(DocumentId, editedText);
        
        if (success)
        {
            document.ExtractedText = editedText;
            isEditing = false;
        }
        
        isSaving = false;
    }

    private string GetStatusBadgeClass(DocumentStatus status)
    {
        return status switch
        {
            DocumentStatus.Uploaded => "bg-secondary",
            DocumentStatus.OcrInProgress => "bg-info",
            DocumentStatus.OcrCompleted => "bg-success",
            DocumentStatus.OcrFailed => "bg-danger",
            DocumentStatus.ProcessingInProgress => "bg-warning",
            DocumentStatus.ProcessingCompleted => "bg-success",
            DocumentStatus.ProcessingFailed => "bg-danger",
            _ => "bg-secondary"
        };
    }

    private bool IsImageFile(string fileName)
    {
        var ext = Path.GetExtension(fileName).ToLowerInvariant();
        return ext == ".jpg" || ext == ".jpeg" || ext == ".png" || ext == ".gif" || ext == ".bmp";
    }

    private bool IsTextFile(string fileName)
    {
        if (string.IsNullOrEmpty(fileName)) return false;
        var ext = Path.GetExtension(fileName).ToLowerInvariant();
        return ext == ".txt";
    }

    private bool IsPdfFile(string fileName)
    {
        return Path.GetExtension(fileName).ToLowerInvariant() == ".pdf";
    }

    private void GoBack()
    {
        if (document?.TestId.HasValue == true)
        {
            Navigation.NavigateTo($"/test/{document.TestId}");
        }
        else
        {
            Navigation.NavigateTo("/documents");
        }
    }

    private string FormatFileSize(long bytes)
    {
        string[] sizes = { "B", "KB", "MB", "GB" };
        double len = bytes;
        int order = 0;
        while (len >= 1024 && order < sizes.Length - 1)
        {
            order++;
            len = len / 1024;
        }
        return $"{len:0.##} {sizes[order]}";
    }
}
